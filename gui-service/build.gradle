description = "Runs demo GUI"

version = '0.0.1'

clean {
  delete 'dist'
}

task install(type: Exec) {
  commandLine 'npm', 'install'
}

task build(type: Exec, overwrite: true) {
  dependsOn install
  commandLine 'npm', 'run-script', 'build'
}

dockerBuildImage.dependsOn build
